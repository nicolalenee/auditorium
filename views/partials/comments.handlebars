<div class="content comments">
  {{#each comments}}
  <div class="comment">
    <div>
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </div>
  {{/each}}
</div>